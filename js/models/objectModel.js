define(
    [
        'backbone'
    ],
    function(
        Backbone
    ) {
        'use strict';

        var ObjectModel;

        ObjectModel = Backbone.Model.extend();

        return ObjectModel;
    }
);
