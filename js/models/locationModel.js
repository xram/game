define(
    [
        'backbone'
    ],
    function(
        Backbone
    ) {
        'use strict';

        var LocationModel;

        LocationModel = Backbone.Model.extend();

        return LocationModel;
    }
);
